---
title: '反向加载历史消息'
description: '使用 getAdvancedHistoryMessageListReverse 加载起点之后的消息。'
sourcePath: '/sdk/android/message/retrieving-messages/load-newer-messages'
---

该方法用于从消息定位点向后读取下一页历史消息。此类方向历史分页使用 `ViewType.History`。

```java
messageManager.getAdvancedHistoryMessageListReverse(callback, conversationID, startMessage, 20, ViewType.History);
```
