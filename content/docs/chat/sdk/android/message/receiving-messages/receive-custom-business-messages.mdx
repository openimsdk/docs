---
title: 'Receive custom business messages'
description: 'Use setCustomBusinessListener to receive business callbacks that are separate from regular chat messages.'
product: 'sdk'
context: 'chat/sdk/android'
template: 'guide'
status: 'published'
lastUpdated: '2026-08-10'
version: 'v4'
platform: 'android'
sourcePath: '/sdk/android/message/receiving-messages/receive-custom-business-messages'
---

The custom business callback carries a string payload. Parse it according to your application format instead of rendering it directly as a `Message`.

```java
messageManager.setCustomBusinessListener(new OnCustomBusinessListener() {
    @Override public void onRecvCustomBusinessMessage(String payload) { handleBusiness(payload); }
});
```
