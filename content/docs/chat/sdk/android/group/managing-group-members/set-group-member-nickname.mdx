---
title: 'Update a group nickname'
description: 'Use SetGroupMemberInfo.nickname to update a member’s display name in a group.'
product: 'sdk'
context: 'chat/sdk/android'
template: 'guide'
status: 'published'
lastUpdated: '2026-08-10'
version: 'v4'
platform: 'android'
sourcePath: '/sdk/android/group/managing-group-members/set-group-member-nickname'
---

All group member profile changes use `setGroupMemberInfo()`. Set the `groupID`, `userID`, and `nickname` in the request.

```java
SetGroupMemberInfo update = new SetGroupMemberInfo();
update.setGroupID(groupID);
update.setUserID(userID);
update.setNickname("Lin");

OpenIMClient.getInstance().groupManager.setGroupMemberInfo(update, new OnBase<String>() {
    @Override public void onSuccess(String data) { markNicknameSubmitted(groupID, userID); }
    @Override public void onError(int code, String error) { showMemberUpdateError(error); }
});
```

Update the displayed name from `GroupMembersInfo.nickname` received by `onGroupMemberInfoChanged`.
