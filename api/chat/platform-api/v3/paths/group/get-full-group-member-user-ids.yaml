post:
  tags:
    - 'Groups'
  summary: 'GroupGetFullGroupMemberUserIds'
  description: 'OpenIM Server POST route handled by g.GetFullGroupMemberUserIDs. It accepts the JSON serialization of group.GetFullGroupMemberUserIDsReq. Documented fields reflect the upstream wire types; handler-specific validation may impose additional constraints.'
  operationId: postGroupGetFullGroupMemberUserIds
  parameters:
    - $ref: ../../components/common/operation-id.yaml
  requestBody:
    required: true
    content:
      application/json:
        schema:
          description: 'JSON serialization of group.GetFullGroupMemberUserIDsReq, matching the upstream wire type.'
          type: object
          additionalProperties: false
          properties:
            'idHash':
              type: integer
              format: int64
            'groupID':
              type: string
          x-source-schema-status: field-level
          x-source-schema-kind: protobuf
          x-source-protobuf-type: 'openim.group.GetFullGroupMemberUserIDsReq'
        example:
          idHash: 42
          groupID: grp_product_2026
  responses:
    '200':
      description: OpenIM result envelope. Inspect errCode; zero indicates success.
      content:
        application/json:
          schema:
            allOf:
              - $ref: ../../components/common/api-envelope.yaml
              - type: object
                properties:
                  data:
                    description: 'Group-member synchronization version and complete member identifier list.'
                    type: object
                    additionalProperties: false
                    properties:
                      'version':
                        description: 'Server version used for synchronization.'
                        type: integer
                        format: int64
                      'versionID':
                        description: 'Identifier paired with the synchronization version.'
                        type: string
                      'equal':
                        description: 'Whether the client version matches the server version.'
                        type: boolean
                      'userIDs':
                        description: 'Identifiers of the returned users.'
                        type: array
                        items:
                          type: string
                    x-source-schema-status: field-level
                    x-source-schema-kind: protobuf
                    x-source-protobuf-type: 'openim.group.GetFullGroupMemberUserIDsResp'
  security:
    - TokenAuth: []
  x-source-handler: 'https://github.com/openimsdk/open-im-server/blob/v3.8.3-patch.16/internal/api/group.go'
  x-source-gin-path: '/group/get_full_group_member_user_ids'
  x-source-request-type: 'group.GetFullGroupMemberUserIDsReq'
  x-source-response-type: 'group.GetFullGroupMemberUserIDsResp'
