---
title: 'Create an audio message from a file'
description: 'OpenIM React Native SDK guide for create an audio message from a file.'
product: 'sdk'
context: 'chat/sdk/react-native'
template: 'guide'
status: 'draft'
version: 'v4'
platform: 'react-native'
sourcePath: '/sdk/react-native/message/creating-messages/create-sound-message-by-file'
---

Use `createSoundMessageFromFullPath()` to create a message from an audio file on the device.

```ts
const message = await OpenIMSDK.createSoundMessageFromFullPath({
  soundPath,
  duration: 18,
});
```

## Parameters

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| `soundPath` | `string` | Yes | Full path to the audio file. |
| `duration` | `number` | Yes | Audio duration in seconds. |

The call returns a `MessageItem` ready to send. When you use [Send a message](/sdk/react-native/message/sending-messages/send-message), the SDK uploads the local audio resource.
