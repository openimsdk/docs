---
title: '从分组移除会话'
description: '从指定会话分组移除一个或多个会话。'
sourcePath: '/sdk/uniapp/conversation/managing-conversation-groups/remove-conversations-from-groups'
---

会话分组属于商业版能力。该操作只删除会话与分组的关联，不删除会话，也不清空消息。

## 参数说明

| 参数 | 类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| `conversationIDs` | `string[]` | 是 | 要移除的 OpenIM 会话 ID。 |
| `conversationGroupIDs` | `string[]` | 是 | 目标会话分组 ID。 |

```uts
import { removeConversationsFromGroups } from '@/uni_modules/unix-openim-sdk'

await removeConversationsFromGroups({
  conversationIDs: [conversationID],
  conversationGroupIDs: [conversationGroupID],
})
```

Promise 成功表示关联移除请求已经完成，不表示分组成员事件已经到达。事件到达后，重新查询受影响的分组或分组内会话并以最新结果为准。
