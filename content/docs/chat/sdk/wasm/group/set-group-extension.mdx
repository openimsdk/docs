---
title: 'Set group extra data'
description: 'Set an application-defined extension string on a group.'
product: 'sdk'
context: 'chat/sdk/wasm'
template: 'guide'
status: 'published'
lastUpdated: '2026-08-21'
version: 'v4'
platform: 'wasm'
sourcePath: '/sdk/wasm/group/set-group-extension'
---

```ts
const previous = JSON.parse(group.ex || '{}');

await openimsdk.setGroupInfo({
  groupID,
  ex: JSON.stringify({
    ...previous,
    projectID: 'project-42',
  }),
});
```

`ex` is a complete string; the SDK does not merge individual JSON fields automatically. If several modules share it, assign stable namespaces and preserve data from other modules before writing.

After the Promise succeeds, use `groupID` to update the matching group profile when `OnGroupInfoChanged` arrives. See [Group overview](/sdk/wasm/group/overview-group) for the complete listener.
