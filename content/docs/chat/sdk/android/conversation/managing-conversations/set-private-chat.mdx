---
title: 'Enable or disable burn after reading'
description: 'Use setOneConversationPrivateChat() to configure burn-after-reading mode for an Android conversation.'
product: 'sdk'
context: 'chat/sdk/android'
template: 'guide'
status: 'published'
lastUpdated: '2026-08-07'
version: 'v4'
platform: 'android'
sourcePath: '/sdk/android/conversation/managing-conversations/set-private-chat'
---

```java
OpenIMClient.getInstance().conversationManager.setOneConversationPrivateChat(
    new OnBase<String>() {
        @Override
        public void onSuccess(String data) {
            markPrivateChatSubmitted(conversationID, true);
        }

        @Override
        public void onError(int code, String error) {
            recordPrivateChatFailure(code, error);
        }
    },
    conversationID,
    true
);
```

Pass `false` to disable burn after reading. Set the duration separately with `burnDuration`; see [Set the burn duration](/sdk/android/conversation/managing-conversations/set-burn-duration). This is distinct from scheduled server message deletion. Reconcile the final state through `onConversationChanged`.
