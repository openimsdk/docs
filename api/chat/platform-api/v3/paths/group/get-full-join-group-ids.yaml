post:
  tags:
    - 'Groups'
  summary: 'GroupGetFullJoinGroupIds'
  description: 'OpenIM Server POST route handled by g.GetFullJoinGroupIDs. It accepts the JSON serialization of group.GetFullJoinGroupIDsReq. Documented fields reflect the upstream wire types; handler-specific validation may impose additional constraints.'
  operationId: postGroupGetFullJoinGroupIds
  parameters:
    - $ref: ../../components/common/operation-id.yaml
  requestBody:
    required: true
    content:
      application/json:
        schema:
          description: 'JSON serialization of group.GetFullJoinGroupIDsReq, matching the upstream wire type.'
          type: object
          additionalProperties: false
          properties:
            'idHash':
              type: integer
              format: int64
            'userID':
              type: string
          x-source-schema-status: field-level
          x-source-schema-kind: protobuf
          x-source-protobuf-type: 'openim.group.GetFullJoinGroupIDsReq'
        example:
          idHash: 42
          userID: user_1001
  responses:
    '200':
      description: OpenIM result envelope. Inspect errCode; zero indicates success.
      content:
        application/json:
          schema:
            allOf:
              - $ref: ../../components/common/api-envelope.yaml
              - type: object
                properties:
                  data:
                    description: 'Joined-group synchronization version and complete group identifier list.'
                    type: object
                    additionalProperties: false
                    properties:
                      'version':
                        description: 'Server version used for synchronization.'
                        type: integer
                        format: int64
                      'versionID':
                        description: 'Identifier paired with the synchronization version.'
                        type: string
                      'equal':
                        description: 'Whether the client version matches the server version.'
                        type: boolean
                      'groupIDs':
                        description: 'Identifiers of the returned groups.'
                        type: array
                        items:
                          type: string
                    x-source-schema-status: field-level
                    x-source-schema-kind: protobuf
                    x-source-protobuf-type: 'openim.group.GetFullJoinGroupIDsResp'
  security:
    - TokenAuth: []
  x-source-handler: 'https://github.com/openimsdk/open-im-server/blob/v3.8.3-patch.16/internal/api/group.go'
  x-source-gin-path: '/group/get_full_join_group_ids'
  x-source-request-type: 'group.GetFullJoinGroupIDsReq'
  x-source-response-type: 'group.GetFullJoinGroupIDsResp'
