---
title: '获取发出的入群申请'
description: '分页查询当前账号发出的入群申请。'
sourcePath: '/sdk/harmony/group/group-applications/get-group-application-list-as-applicant'
---

查询自己发出的申请时，将 `send` 设为 `true`。

```ts
import sdk, { GetGroupApplicationReq, GetGroupApplicationResp } from '@openimsdk/imsdk';

const request: GetGroupApplicationReq = {
  send: true,
  groupIDs: [],
  handleResults: [],
  pagination: { pageNumber: 1, showNumber: 20 },
};
const response: GetGroupApplicationResp = await sdk.getGroupApplication(request);
const applications = response.applications;
```

`groupIDs` 和 `handleResults` 为空数组时不限定群组和状态。申请字段、稳定标识和事件处理方式见[获取收到的入群申请](/sdk/harmony/group/group-applications/get-group-application-list-as-recipient)。分页期间收到状态变化事件时，应重置分页并重新加载。
