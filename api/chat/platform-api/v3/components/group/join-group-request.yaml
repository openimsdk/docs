additionalProperties: false
properties:
  ex:
    type: string
  groupID:
    type: string
    minLength: 1
  inviterUserID:
    type: string
    minLength: 1
    description: User whose relationship and role are used to process the join request.
  joinSource:
    format: int32
    type: integer
    minimum: 1
    maximum: 4
  reqMessage:
    type: string
type: object
required:
  - groupID
  - inviterUserID
  - joinSource
