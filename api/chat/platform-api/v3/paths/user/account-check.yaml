post:
  tags:
    - 'Users'
  x-export-category: Public
  summary: 'UserAccountCheck'
  description: 'OpenIM Server POST route handled by u.AccountCheck. It accepts the JSON serialization of user.AccountCheckReq. Documented fields reflect the upstream wire types; handler-specific validation may impose additional constraints.'
  operationId: postUserAccountCheck
  parameters:
    - $ref: ../../components/common/operation-id.yaml
  requestBody:
    required: true
    content:
      application/json:
        schema:
          description: 'JSON serialization of user.AccountCheckReq, matching the upstream wire type.'
          type: object
          additionalProperties: false
          properties:
            'checkUserIDs':
              type: array
              items:
                type: string
          x-source-schema-status: field-level
          x-source-schema-kind: protobuf
          x-source-protobuf-type: 'openim.user.accountCheckReq'
        example:
          checkUserIDs:
            - user_1001
            - user_1002
  responses:
    '200':
      description: OpenIM result envelope. Inspect errCode; zero indicates success.
      content:
        application/json:
          schema:
            allOf:
              - $ref: ../../components/common/api-envelope.yaml
              - type: object
                properties:
                  data:
                    description: Registration statuses for the requested user IDs.
                    type: object
                    additionalProperties: false
                    properties:
                      'results':
                        description: Account check results by user.
                        type: array
                        items:
                          type: object
                          additionalProperties: false
                          properties:
                            'userID':
                              description: Unique user identifier.
                              type: string
                            'accountStatus':
                              description: Registration status returned by the account check.
                              type: integer
                              format: int32
                          x-source-schema-status: field-level
                          x-source-schema-kind: protobuf
                          x-source-protobuf-type: 'openim.user.accountCheckResp.singleUserStatus'
                    x-source-schema-status: field-level
                    x-source-schema-kind: protobuf
                    x-source-protobuf-type: 'openim.user.accountCheckResp'
  security:
    - TokenAuth: []
  x-source-handler: 'https://github.com/openimsdk/open-im-server/blob/v3.8.3-patch.16/internal/api/user.go'
  x-source-gin-path: '/user/account_check'
  x-source-request-type: 'user.AccountCheckReq'
  x-source-response-type: 'user.AccountCheckResp'
