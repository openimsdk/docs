post:
  tags:
    - 'Friends'
  summary: 'FriendIsFriend'
  description: 'OpenIM Server POST route handled by f.IsFriend. It accepts the JSON serialization of relation.IsFriendReq. Documented fields reflect the upstream wire types; handler-specific validation may impose additional constraints.'
  operationId: postFriendIsFriend
  parameters:
    - $ref: ../../components/common/operation-id.yaml
  requestBody:
    required: true
    content:
      application/json:
        schema:
          description: 'JSON serialization of relation.IsFriendReq, matching the upstream wire type.'
          type: object
          additionalProperties: false
          properties:
            'userID1':
              type: string
            'userID2':
              type: string
          x-source-schema-status: field-level
          x-source-schema-kind: protobuf
          x-source-protobuf-type: 'openim.relation.isFriendReq'
        example:
          userID1: user_1001
          userID2: user_2002
  responses:
    '200':
      description: OpenIM result envelope. Inspect errCode; zero indicates success.
      content:
        application/json:
          schema:
            allOf:
              - $ref: ../../components/common/api-envelope.yaml
              - type: object
                properties:
                  data:
                    description: Friendship relationship results for the two users.
                    type: object
                    additionalProperties: false
                    properties:
                      'inUser1Friends':
                        description: Whether the second user is in the first user's friend list.
                        type: boolean
                      'inUser2Friends':
                        description: Whether the first user is in the second user's friend list.
                        type: boolean
                    x-source-schema-status: field-level
                    x-source-schema-kind: protobuf
                    x-source-protobuf-type: 'openim.relation.isFriendResp'
  security:
    - TokenAuth: []
  x-source-handler: 'https://github.com/openimsdk/open-im-server/blob/v3.8.3-patch.16/internal/api/friend.go'
  x-source-gin-path: '/friend/is_friend'
  x-source-request-type: 'relation.IsFriendReq'
  x-source-response-type: 'relation.IsFriendResp'
