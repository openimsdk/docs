---
title: '设置群成员扩展字段'
description: '使用 SetGroupMemberInfo.ex 保存应用定义的群成员扩展数据。'
sourcePath: '/sdk/android/group/managing-group-members/set-group-member-extension'
---

`SetGroupMemberInfo.ex` 字符串用于保存群成员扩展信息，可存储 JSON 字符串拓展群成员信息。

```java
SetGroupMemberInfo update = new SetGroupMemberInfo();
update.setGroupID(groupID);
update.setUserID(userID);
update.setEx("{\"duty\":\"reviewer\"}");

OpenIMClient.getInstance().groupManager.setGroupMemberInfo(update, new OnBase<String>() {
    @Override public void onSuccess(String data) { markMemberExtensionSubmitted(groupID, userID); }
    @Override public void onError(int code, String error) { showMemberUpdateError(error); }
});
```
