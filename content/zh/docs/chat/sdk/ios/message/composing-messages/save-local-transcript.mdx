---
title: '保存本地转写结果'
description: '使用 iOS SDK 把转写结果保存到当前设备的消息扩展字段。'
sourcePath: '/sdk/ios/message/composing-messages/save-local-transcript'
---

`OIMSoundElem` 没有转写文字属性，应先解析并合并原 `localEx`，再写入业务命名空间：

```objc
NSString *localEx = [self mergeTranscript:transcript intoLocalEx:message.localEx];
[[OIMManager manager] setMessageLocalEx:conversationID
    clientMsgID:message.clientMsgID
    localEx:localEx
    onSuccess:nil
    onFailure:nil];
```

已有值不是合法 JSON 对象时按产品约定中止或迁移，不能静默覆盖。成功只修改当前设备，不同步给其他用户或设备，也不触发消息事件。
