post:
  tags:
    - 'Friends'
  summary: 'FriendGetFullFriendUserIds'
  description: 'OpenIM Server POST route handled by f.GetFullFriendUserIDs. It accepts the JSON serialization of relation.GetFullFriendUserIDsReq. Documented fields reflect the upstream wire types; handler-specific validation may impose additional constraints.'
  operationId: postFriendGetFullFriendUserIds
  parameters:
    - $ref: ../../components/common/operation-id.yaml
  requestBody:
    required: true
    content:
      application/json:
        schema:
          description: 'JSON serialization of relation.GetFullFriendUserIDsReq, matching the upstream wire type.'
          type: object
          additionalProperties: false
          properties:
            'idHash':
              type: integer
              format: int64
            'userID':
              type: string
          x-source-schema-status: field-level
          x-source-schema-kind: protobuf
          x-source-protobuf-type: 'openim.relation.getFullFriendUserIDsReq'
        example:
          idHash: 42
          userID: user_1001
  responses:
    '200':
      description: OpenIM result envelope. Inspect errCode; zero indicates success.
      content:
        application/json:
          schema:
            allOf:
              - $ref: ../../components/common/api-envelope.yaml
              - type: object
                properties:
                  data:
                    description: Full friend user-ID synchronization result.
                    type: object
                    additionalProperties: false
                    properties:
                      'version':
                        description: Synchronization version.
                        type: integer
                        format: int64
                      'versionID':
                        description: Server synchronization version identifier.
                        type: string
                      'equal':
                        description: Whether the client and server versions match.
                        type: boolean
                      'userIDs':
                        description: User identifiers returned by this query.
                        type: array
                        items:
                          type: string
                    x-source-schema-status: field-level
                    x-source-schema-kind: protobuf
                    x-source-protobuf-type: 'openim.relation.getFullFriendUserIDsResp'
  security:
    - TokenAuth: []
  x-source-handler: 'https://github.com/openimsdk/open-im-server/blob/v3.8.3-patch.16/internal/api/friend.go'
  x-source-gin-path: '/friend/get_full_friend_user_ids'
  x-source-request-type: 'relation.GetFullFriendUserIDsReq'
  x-source-response-type: 'relation.GetFullFriendUserIDsResp'
