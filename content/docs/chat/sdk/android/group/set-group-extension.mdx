---
title: 'Set group extra data'
description: 'Store application-defined group data in GroupInfo.ex.'
product: 'sdk'
context: 'chat/sdk/android'
template: 'guide'
status: 'published'
lastUpdated: '2026-08-10'
version: 'v4'
platform: 'android'
sourcePath: '/sdk/android/group/set-group-extension'
---

`GroupInfo.ex` is a complete string; the SDK does not merge JSON values. If several application modules share the field, read the existing value and preserve the other namespaces when merging your changes.

```java
GroupInfo update = new GroupInfo();
update.setGroupID(groupID);
update.setEx("{\"projectCode\":\"mobile\",\"archived\":false}");

OpenIMClient.getInstance().groupManager.setGroupInfo(update, new OnBase<String>() {
    @Override public void onSuccess(String data) { markExtensionSubmitted(groupID); }
    @Override public void onError(int code, String error) { showExtensionError(error); }
});
```

Do not store access tokens, secrets, or device-only UI state in group extra data.
