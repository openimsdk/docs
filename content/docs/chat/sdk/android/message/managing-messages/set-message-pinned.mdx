---
title: 'Pin or unpin a message'
description: 'Change a conversation message’s pinned state and handle pinned-message change events.'
product: 'sdk'
context: 'chat/sdk/android'
template: 'guide'
status: 'published'
lastUpdated: '2026-08-17'
version: 'v4'
platform: 'android'
sourcePath: '/sdk/android/message/managing-messages/set-message-pinned'
---

Pass `true` as the final argument to pin the message or `false` to unpin it:

```java
messageManager.setConversationPinnedMsg(
    callback,
    conversationID,
    clientMsgID,
    true
);
```

The success callback means that the request completed; it does not mean that the pinned-message event has arrived.

When pinned messages change, `OnAdvanceMsgListener.onChangedPinnedMsg` returns the corresponding `conversationID` and current list of pinned messages:

```java
@Override
public void onChangedPinnedMsg(ChangedPinnedMsg event) {
    handlePinnedMessagesChanged(event.getConversationID(), event.getMsgs());
}
```

`MessageManager` can retain only one `OnAdvanceMsgListener`. If the application handles other message events, implement their callbacks in the same listener. See [Events](/sdk/android/events/overview-events) for setup. To retrieve the current pinned messages directly, use [View pinned messages](/sdk/android/message/managing-messages/get-pinned-messages).
