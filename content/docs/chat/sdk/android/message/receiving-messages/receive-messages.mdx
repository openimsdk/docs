---
title: 'Receive messages'
description: 'Use OnAdvanceMsgListener to receive new, offline, and online-only messages.'
product: 'sdk'
context: 'chat/sdk/android'
template: 'guide'
status: 'published'
lastUpdated: '2026-08-10'
version: 'v4'
platform: 'android'
sourcePath: '/sdk/android/message/receiving-messages/receive-messages'
---

After calling `setAdvancedMsgListener()`, `onRecvNewMessage` receives real-time messages, `onRecvOfflineNewMessage` receives offline batches, and `onRecvOnlineOnlyMessage` receives online-only messages.

```java
messageManager.setAdvancedMsgListener(new OnAdvanceMsgListener() {
    @Override public void onRecvNewMessage(Message message) { messageStore.merge(message); }
    @Override public void onRecvOfflineNewMessage(List<Message> messages) { messageStore.mergeAll(messages); }
});
```
