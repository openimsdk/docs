---
title: '获取通话房间凭据'
description: '按房间 ID 重新获取加入媒体房间所需的凭据。'
sourcePath: '/sdk/uniapp/calling/retrieving-call-information/get-token-by-room-id'
---

已经知道 `roomID`，但需要重新取得入会 Token 时，调用 `signalingGetTokenByRoomID()`：

```uts
import {
  signalingGetTokenByRoomID,
} from '@/uni_modules/unix-openim-sdk'

const result = await signalingGetTokenByRoomID({ roomID })

if (result?.token != null) {
  await mediaEngineJoin(roomID, result.token!, result.liveURL)
}
```

Promise 成功后返回 `OpenIMSignalingGetTokenByRoomIDResult | null`，其中 `token` 和 `liveURL` 都可能为空。返回结果不重复包含 `roomID`；连接媒体引擎时继续使用本次查询参数中的值。

Token 是短期敏感凭据，只应保存在内存中。不要把它写入日志、URL、分析事件或持久化存储；取得不到有效 Token 时不要尝试进入媒体房间。
