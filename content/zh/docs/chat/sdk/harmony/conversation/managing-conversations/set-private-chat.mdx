---
title: '开启或关闭阅后即焚'
description: '设置指定会话是否启用阅后即焚模式。'
sourcePath: '/sdk/harmony/conversation/managing-conversations/set-private-chat'
---

阅后即焚属于商业版能力，与定期删除服务端消息不是同一项策略。

```ts
import sdk, { SetConversationReq } from '@openimsdk/imsdk';

const request: SetConversationReq = {
  conversationID: conversationID,
  isPrivateChat: true,
};
await sdk.setConversation(request);
```

传 `false` 关闭。消息保留时长通过 `burnDuration` 单独设置。Promise 成功后等待会话变化事件。
