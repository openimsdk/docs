---
title: 'Load message history in reverse'
description: 'Retrieve history from a message anchor toward newer messages.'
product: 'sdk'
context: 'chat/sdk/wasm'
template: 'guide'
status: 'published'
lastUpdated: '2026-08-10'
version: 'v4'
platform: 'wasm'
sourcePath: '/sdk/wasm/message/retrieving-messages/load-newer-messages'
---

Use the reverse history API when you already have a message anchor and need to fill in messages that come after it.

## Parameters

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| `conversationID` | `string` | Yes | ID of the conversation whose messages to retrieve. |
| `startClientMsgID` | `string` | Yes | Anchor message ID from which to read toward newer messages. |
| `count` | `number` | Yes | Maximum number of messages to retrieve. |
| `viewType` | `MessageViewType` | Yes | Message viewing direction. Use `MessageViewType.History` when loading history. |

```ts
import { MessageViewType } from '@openim/wasm-client-sdk';

const { data: page } =
  await openimsdk.getAdvancedHistoryMessageListReverse({
    conversationID,
    startClientMsgID: anchorMessage.clientMsgID,
    count: 30,
    viewType: MessageViewType.History,
  });
```

When the Promise resolves, `data` is an `AdvancedMessageListResult`. For field descriptions and result-status handling, see [Load message history](/sdk/wasm/message/retrieving-messages/load-older-messages). On this page, `isEnd` indicates whether the query has reached the boundary in the newer-message direction.

Do not mix both loading directions at the same scroll boundary. Deduplicate merged results by `conversationID:clientMsgID`.
