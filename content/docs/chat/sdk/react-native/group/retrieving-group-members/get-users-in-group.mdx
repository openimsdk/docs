---
title: 'Check users in a group'
description: 'OpenIM React Native SDK guide for check users in a group.'
product: 'sdk'
context: 'chat/sdk/react-native'
template: 'guide'
status: 'draft'
version: 'v4'
platform: 'react-native'
sourcePath: '/sdk/react-native/group/retrieving-group-members/get-users-in-group'
---

Call `getUsersInGroup()` to retrieve the user IDs from a specified set that have joined a group.

```ts
const userIDs = await OpenIMSDK.getUsersInGroup({
  groupID: 'group-001',
  userIDList: ['user-001', 'user-002', 'user-003'],
});
```

## Parameters

| Parameter | Type | Required | Description |
| --- | --- | --- |
| `groupID` | `string` | Yes | ID of the target group. |
| `userIDList` | `string[]` | Yes | User IDs to check. |

## Return value

When the call completes, it returns `string[]` containing the user IDs that have joined the target group.
