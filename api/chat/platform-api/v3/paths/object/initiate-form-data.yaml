post:
  tags:
    - 'Object storage'
  summary: 'ObjectInitiateFormData'
  description: 'OpenIM Server POST route handled by t.InitiateFormData. It accepts the JSON serialization of third.InitiateFormDataReq. Documented fields reflect the upstream wire types; handler-specific validation may impose additional constraints.'
  operationId: postObjectInitiateFormData
  parameters:
    - $ref: ../../components/common/operation-id.yaml
  requestBody:
    required: true
    content:
      application/json:
        schema:
          description: 'JSON serialization of third.InitiateFormDataReq, matching the upstream wire type.'
          type: object
          additionalProperties: false
          properties:
            'name':
              type: string
            'size':
              type: integer
              format: int64
            'contentType':
              type: string
            'group':
              type: string
            'millisecond':
              type: integer
              format: int64
            'absolute':
              type: boolean
          x-source-schema-status: field-level
          x-source-schema-kind: protobuf
          x-source-protobuf-type: 'openim.third.InitiateFormDataReq'
        example:
          name: avatar.png
          size: 2048
          contentType: image/png
          group: avatars
          millisecond: 1710000000123
          absolute: false
  responses:
    '200':
      description: OpenIM result envelope. Inspect errCode; zero indicates success.
      content:
        application/json:
          schema:
            allOf:
              - $ref: ../../components/common/api-envelope.yaml
              - type: object
                properties:
                  data:
                    type: object
                    description: Form-upload credentials and submission parameters.
                    additionalProperties: false
                    properties:
                      'id':
                        type: string
                        description: Upload task ID used to confirm the completed upload.
                      'url':
                        type: string
                        description: Third-party storage URL for the direct form upload.
                      'file':
                        type: string
                        description: Multipart form field name that carries the file content.
                      'header':
                        type: array
                        description: HTTP headers to add to the direct-upload request.
                        items:
                          type: object
                          additionalProperties: false
                          properties:
                            'key':
                              type: string
                              description: HTTP header name.
                            'values':
                              type: array
                              description: Values for the HTTP header.
                              items:
                                type: string
                          x-source-schema-status: field-level
                          x-source-schema-kind: protobuf
                          x-source-protobuf-type: 'openim.third.KeyValues'
                      'formData':
                        type: array
                        description: Form fields to submit unchanged with the upload.
                        items:
                          type: object
                          additionalProperties: false
                          properties:
                            'key':
                              type: string
                              description: Form field name.
                            'value':
                              type: string
                              description: Form field value.
                          x-source-schema-status: field-level
                          x-source-schema-kind: protobuf
                          x-source-protobuf-type: 'openim.third.InitiateFormDataResp.FormDataEntry'
                      'expires':
                        type: integer
                        format: int64
                        description: Credential expiration timestamp in milliseconds.
                      'successCodes':
                        type: array
                        description: HTTP status codes accepted as successful direct-upload results.
                        items:
                          type: integer
                          format: int32
                    x-source-schema-status: field-level
                    x-source-schema-kind: protobuf
                    x-source-protobuf-type: 'openim.third.InitiateFormDataResp'
  security:
    - TokenAuth: []
  x-source-handler: 'https://github.com/openimsdk/open-im-server/blob/v3.8.3-patch.16/internal/api/third.go'
  x-source-gin-path: '/object/initiate_form_data'
  x-source-request-type: 'third.InitiateFormDataReq'
  x-source-response-type: 'third.InitiateFormDataResp'
