---
title: 'Insert a local group message'
description: 'Use insertGroupMessageToLocalStorage to insert a group message visible only on the current device.'
product: 'sdk'
context: 'chat/sdk/android'
template: 'guide'
status: 'published'
lastUpdated: '2026-08-10'
version: 'v4'
platform: 'android'
sourcePath: '/sdk/android/message/managing-messages/insert-local-group-message'
---

`insertGroupMessageToLocalStorage()` writes a message to a group chat on the current device. Use it for imported records or application notices that must remain local. It does not send the message or synchronize it to other devices.

`message` is the complete message object, `groupID` identifies the target group, and `senderUserID` is the sender shown for the inserted message.

```java
OpenIMClient.getInstance().messageManager.insertGroupMessageToLocalStorage(
    new OnBase<String>() {
        @Override
        public void onSuccess(String data) {
            // Re-read or update the group message list on this device.
        }

        @Override
        public void onError(int code, String error) {
            // Report the local write failure.
        }
    },
    message,
    groupID,
    senderUserID
);
```

The success callback means the local write completed. This operation does not trigger a new-message event and cannot replace [Send a message](/sdk/android/message/sending-messages/send-message).
