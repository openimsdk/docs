---
title: 'Reject a friend application'
description: 'Reject a friend request with the OpenIM React Native SDK.'
product: 'sdk'
context: 'chat/sdk/react-native'
template: 'guide'
status: 'published'
version: 'v4'
platform: 'react-native'
sourcePath: '/sdk/react-native/user/friend-applications/refuse-friend-application'
---

Call `refuseFriendApplication()` to reject a friend request.

```ts
await OpenIMSDK.refuseFriendApplication({
  toUserID: applicantUserID,
  handleMsg: '',
});
```

## Parameter description

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| `toUserID` | `string` | Yes | User ID of the applicant. |
| `handleMsg` | `string` | Yes | Message attached when handling the request. Pass an empty string when there is no message. |

For request status changes, see the listener examples in [Get received friend requests](/sdk/react-native/user/friend-applications/get-friend-application-list-as-recipient).
