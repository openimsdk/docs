---
title: 'Set group member extra data'
description: 'Store application-defined group member data in SetGroupMemberInfo.ex.'
product: 'sdk'
context: 'chat/sdk/android'
template: 'guide'
status: 'published'
lastUpdated: '2026-08-10'
version: 'v4'
platform: 'android'
sourcePath: '/sdk/android/group/managing-group-members/set-group-member-extension'
---

`SetGroupMemberInfo.ex` is a complete string; the SDK does not merge JSON values. If several application modules share the field, read and merge the existing value while preserving the other namespaces.

```java
SetGroupMemberInfo update = new SetGroupMemberInfo();
update.setGroupID(groupID);
update.setUserID(userID);
update.setEx("{\"duty\":\"reviewer\"}");

OpenIMClient.getInstance().groupManager.setGroupMemberInfo(update, new OnBase<String>() {
    @Override public void onSuccess(String data) { markMemberExtensionSubmitted(groupID, userID); }
    @Override public void onError(int code, String error) { showMemberUpdateError(error); }
});
```

Do not store credentials, tokens, or device-only display state in this field.
