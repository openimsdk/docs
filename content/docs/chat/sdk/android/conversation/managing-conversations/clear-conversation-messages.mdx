---
title: 'Clear messages in a conversation'
description: 'Use messageManager.clearConversationAndDeleteAllMsg() to keep a conversation entry while clearing its messages.'
product: 'sdk'
context: 'chat/sdk/android'
template: 'guide'
status: 'published'
lastUpdated: '2026-08-07'
version: 'v4'
platform: 'android'
sourcePath: '/sdk/android/conversation/managing-conversations/clear-conversation-messages'
---

`messageManager.clearConversationAndDeleteAllMsg()` clears the messages in a conversation while keeping its entry.

```java
OpenIMClient.getInstance().messageManager.clearConversationAndDeleteAllMsg(
    conversationID,
    new OnBase<String>() {
        @Override
        public void onSuccess(String data) {
            messageStore.clearConversation(conversationID);
        }

        @Override
        public void onError(int code, String error) {
            recordConversationClearFailure(code, error);
        }
    }
);
```

The conversation entry remains, while the current account's messages are cleared. To update the conversation list, query conversations again or receive changes through `onConversationChanged`.
