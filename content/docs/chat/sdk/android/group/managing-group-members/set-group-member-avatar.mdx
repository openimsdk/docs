---
title: 'Update a group member avatar'
description: 'Use SetGroupMemberInfo.faceURL to update the avatar displayed for a member in a group.'
product: 'sdk'
context: 'chat/sdk/android'
template: 'guide'
status: 'published'
lastUpdated: '2026-08-10'
version: 'v4'
platform: 'android'
sourcePath: '/sdk/android/group/managing-group-members/set-group-member-avatar'
---

A group member avatar is a group-specific profile field and does not change the user's global avatar. Pass an avatar URL that group members can access.

```java
SetGroupMemberInfo update = new SetGroupMemberInfo();
update.setGroupID(groupID);
update.setUserID(userID);
update.setFaceURL(avatarURL);

OpenIMClient.getInstance().groupManager.setGroupMemberInfo(update, new OnBase<String>() {
    @Override public void onSuccess(String data) { markAvatarSubmitted(groupID, userID); }
    @Override public void onError(int code, String error) { showMemberUpdateError(error); }
});
```

Keep the previous avatar or an optimistic local state until the event arrives, then reconcile it with the latest member profile.
