---
title: 'Remove group members'
description: 'Use the Android SDK to remove specified members from a group.'
product: 'sdk'
context: 'chat/sdk/android'
template: 'guide'
status: 'published'
lastUpdated: '2026-08-18'
version: 'v4'
platform: 'android'
sourcePath: '/sdk/android/group/managing-group-members/kick-group-member'
---

`kickGroupMember()` accepts a list of member IDs and a removal message.

```java
OpenIMClient.getInstance().groupManager.kickGroupMember(new OnBase<String>() {
    @Override public void onSuccess(String data) { /* The request completed. */ }
    @Override public void onError(int code, String error) { /* code and error describe the failure. */ }
}, groupID, memberUserIDs, "No longer participating in this project");
```

A successful callback means that the removal request completed; it does not mean that every client has updated its UI. `onGroupMemberDeleted` provides the removed member's `GroupMembersInfo`. Remove the member state by `groupID:userID`. See [Group overview](/sdk/android/group/overview-group) for the complete listener setup.
