post:
  tags:
    - 'Authentication'
  x-export-category: Public
  summary: 'AuthGetAdminToken'
  description: 'OpenIM Server POST route handled by a.GetAdminToken. It accepts the JSON serialization of auth.GetAdminTokenReq. Documented fields reflect the upstream wire types; handler-specific validation may impose additional constraints.'
  operationId: postAuthGetAdminToken
  parameters:
    - $ref: ../../components/common/operation-id.yaml
  requestBody:
    required: true
    content:
      application/json:
        schema:
          description: 'JSON serialization of auth.GetAdminTokenReq, matching the upstream wire type.'
          type: object
          additionalProperties: false
          properties:
            'secret':
              type: string
            'userID':
              type: string
          x-source-schema-status: field-level
          x-source-schema-kind: protobuf
          x-source-protobuf-type: 'openim.auth.getAdminTokenReq'
        example:
          secret: admin-secret-2026
          userID: admin_001
  responses:
    '200':
      description: OpenIM result envelope. Inspect errCode; zero indicates success.
      content:
        application/json:
          schema:
            allOf:
              - $ref: ../../components/common/api-envelope.yaml
              - type: object
                properties:
                  data:
                    description: Administrator token and its configured lifetime.
                    type: object
                    additionalProperties: false
                    properties:
                      'token':
                        description: Authentication token.
                        type: string
                      'expireTimeSeconds':
                        description: Configured administrator token lifetime in seconds.
                        type: integer
                        format: int64
                    x-source-schema-status: field-level
                    x-source-schema-kind: protobuf
                    x-source-protobuf-type: 'openim.auth.getAdminTokenResp'
  security: []
  x-source-handler: 'https://github.com/openimsdk/open-im-server/blob/v3.8.3-patch.16/internal/api/auth.go'
  x-source-gin-path: '/auth/get_admin_token'
  x-source-request-type: 'auth.GetAdminTokenReq'
  x-source-response-type: 'auth.GetAdminTokenResp'
