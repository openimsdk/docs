---
title: 'Get a group call room'
description: 'Use the Android SDK to retrieve an active call room by group ID.'
product: 'sdk'
context: 'chat/sdk/android'
template: 'guide'
status: 'published'
lastUpdated: '2026-08-10'
version: 'v4'
platform: 'android'
sourcePath: '/sdk/android/calling/retrieving-call-information/get-room-by-group-id'
---

`signalingGetRoomByGroupID()` takes a group ID, not `roomID`, even when the call uses a custom `roomID`.

```java
OpenIMClient.getInstance().signalingManager.signalingGetRoomByGroupID(
    new OnBase<RoomCallingInfo>() {
        @Override
        public void onSuccess(RoomCallingInfo room) {
            if (room.getRoomID() != null && !room.getRoomID().isEmpty()) {
                renderParticipants(room.getParticipant());
            }
        }

        @Override
        public void onError(int code, String error) {
            showNoActiveGroupCall();
        }
    },
    groupID
);
```

The success callback returns a `RoomCallingInfo` snapshot containing the room ID, original invitation, participants, group ID, token, and connection address.

If there is no active call in the group, the API can return an error or an empty room. Clear locally saved call state and room credentials, and do not attempt to join the old room.
