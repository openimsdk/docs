---
title: 'Send a friend request'
description: 'Use addFriend() to send a friend request to a specified user.'
product: 'sdk'
context: 'chat/sdk/android'
template: 'guide'
status: 'published'
lastUpdated: '2026-08-06'
version: 'v4'
platform: 'android'
sourcePath: '/sdk/android/user/friend-applications/add-friend'
---

```java
OpenIMClient.getInstance().friendshipManager.addFriend(
    new OnBase<String>() {
        @Override
        public void onSuccess(String data) {
            // Friend request sent successfully.
        }

        @Override
        public void onError(int code, String error) {
            // Handle the error.
        }
    },
    "user_b",
    "Hello, I am Alex"
);
```

The second argument is the recipient's `userID`, and the third argument is the request message. A successful callback means the server received the request, not that the recipient accepted it. The target ID cannot be empty. Let a trusted application backend handle lookup by nickname, phone number, or other private fields.

Receive friend request changes through `OnFriendshipListener`, as documented in [Get received friend requests](/sdk/android/user/friend-applications/get-recv-friend-application-list).
