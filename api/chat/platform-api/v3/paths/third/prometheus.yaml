get:
  tags:
    - 'Third-party services'
  summary: 'ThirdPrometheus'
  description: 'OpenIM Server GET route handled by t.GetPrometheus. Documented fields reflect the upstream wire types; handler-specific validation may impose additional constraints.'
  operationId: getThirdPrometheus
  parameters:
    - $ref: ../../components/common/operation-id.yaml
  responses:
    '302':
      description: Redirect to the configured upstream resource.
      headers:
        Location:
          description: Redirect target.
          schema:
            type: string
            format: uri
  security: []
  x-source-handler: 'https://github.com/openimsdk/open-im-server/blob/v3.8.3-patch.16/internal/api/third.go'
  x-source-gin-path: '/third/prometheus'
