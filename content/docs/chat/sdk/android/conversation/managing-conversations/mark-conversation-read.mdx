---
title: 'Mark a conversation as read'
description: 'Use messageManager.markConversationMessageAsRead() to mark conversation messages as read.'
product: 'sdk'
context: 'chat/sdk/android'
template: 'guide'
status: 'published'
lastUpdated: '2026-08-07'
version: 'v4'
platform: 'android'
sourcePath: '/sdk/android/conversation/managing-conversations/mark-conversation-read'
---

After the user has opened and read visible messages, call `messageManager.markConversationMessageAsRead()` to mark the conversation as read. Do not mark a conversation read merely because a notification was previewed or a message arrived in the background.

```java
OpenIMClient.getInstance().messageManager.markConversationMessageAsRead(
    conversationID,
    new OnBase<String>() {
        @Override
        public void onSuccess(String data) {
            markConversationReadSubmitted(conversationID);
        }

        @Override
        public void onError(int code, String error) {
            recordMarkReadFailure(code, error);
        }
    }
);
```

The callback indicates that the mark-as-read request has completed. To update the conversation list, query conversations again or receive changes through `onConversationChanged`.

For one-to-one chats, listen for read receipts from the peer through `OnAdvanceMsgListener.onRecvC2CReadReceipt`. See [Receive messages](/sdk/android/message/receiving-messages/receive-messages) for the message listener setup and [Message overview](/sdk/android/message/overview-message) for message callbacks and read-receipt fields.
