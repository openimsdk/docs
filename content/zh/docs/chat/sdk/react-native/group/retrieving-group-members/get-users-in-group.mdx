---
title: '查询用户入群状态'
description: '查询一组用户中哪些用户属于指定群组。'
product: 'sdk'
context: 'chat/sdk/react-native'
template: 'guide'
status: 'draft'
version: 'v4'
platform: 'react-native'
sourcePath: '/sdk/react-native/group/retrieving-group-members/get-users-in-group'
---

调用 `getUsersInGroup()` 查询指定用户中已加入目标群组的用户 ID。

```ts
const userIDs = await OpenIMSDK.getUsersInGroup({
  groupID: 'group-001',
  userIDList: ['user-001', 'user-002', 'user-003'],
});
```

## 参数说明

| 参数 | 类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| `groupID` | `string` | 是 | 目标群组 ID。 |
| `userIDList` | `string[]` | 是 | 要检查的用户 ID 数组。 |

## 返回结果

调用完成后返回 `string[]`，其中包含已加入目标群组的用户 ID。
