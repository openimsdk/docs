---
title: '设置全局消息接收方式'
description: '通过 setSelfInfo() 设置当前账号的全局消息接收与通知策略。'
sourcePath: '/sdk/android/user/user-profile/set-global-message-reception'
---

使用 `UserInfoReq` 设置 `globalRecvMsgOpt`。

```java
UserInfoReq request = new UserInfoReq();
request.setGlobalRecvMsgOpt(2);

OpenIMClient.getInstance().userInfoManager.setSelfInfo(
    new OnBase<String>() {
        @Override
        public void onSuccess(String data) {
            // 设置全局消息接收方式成功
        }

        @Override
        public void onError(int code, String error) {
            // 处理错误
        }
    },
    request
);
```

| 数值 | 含义                     |
| ---- | ------------------------ |
| `0`  | 正常接收消息并允许通知。 |
| `1`  | 屏蔽消息。               |
| `2`  | 接收消息但不提示。       |

如果产品只提供“全局免打扰”开关，可以在 `0` 和 `2` 之间切换；`1` 会停止接收消息，应单独展示。单个会话的接收方式由会话 API 管理。

成功 callback 表示设置请求完成。
