post:
  tags:
    - 'Object storage'
  summary: 'ObjectAccessUrl'
  description: 'OpenIM Server POST route handled by t.AccessURL. It accepts the JSON serialization of third.AccessURLReq. Documented fields reflect the upstream wire types; handler-specific validation may impose additional constraints.'
  operationId: postObjectAccessUrl
  parameters:
    - $ref: ../../components/common/operation-id.yaml
  requestBody:
    required: true
    content:
      application/json:
        schema:
          description: 'JSON serialization of third.AccessURLReq, matching the upstream wire type.'
          type: object
          additionalProperties: false
          properties:
            'name':
              type: string
            'query':
              type: object
              additionalProperties:
                type: string
          x-source-schema-status: field-level
          x-source-schema-kind: protobuf
          x-source-protobuf-type: 'openim.third.AccessURLReq'
        example:
          name: avatar.png
          query:
            response-content-type: image/png
            download: '1'
  responses:
    '200':
      description: OpenIM result envelope. Inspect errCode; zero indicates success.
      content:
        application/json:
          schema:
            allOf:
              - $ref: ../../components/common/api-envelope.yaml
              - type: object
                properties:
                  data:
                    type: object
                    description: Object access URL and expiration information.
                    additionalProperties: false
                    properties:
                      'url':
                        type: string
                        description: URL used to access the named object.
                      'expireTime':
                        type: integer
                        format: int64
                        description: Unix timestamp in milliseconds when the access URL expires.
                    x-source-schema-status: field-level
                    x-source-schema-kind: protobuf
                    x-source-protobuf-type: 'openim.third.AccessURLResp'
  security:
    - TokenAuth: []
  x-source-handler: 'https://github.com/openimsdk/open-im-server/blob/v3.8.3-patch.16/internal/api/third.go'
  x-source-gin-path: '/object/access_url'
  x-source-request-type: 'third.AccessURLReq'
  x-source-response-type: 'third.AccessURLResp'
