---
title: 'Search groups'
description: 'Use searchGroups to search locally available groups by group ID or name.'
product: 'sdk'
context: 'chat/sdk/android'
template: 'guide'
status: 'published'
lastUpdated: '2026-08-10'
version: 'v4'
platform: 'android'
sourcePath: '/sdk/android/group/retrieving-groups/search-groups'
---

`searchGroups()` accepts a keyword list and separate switches for searching group IDs and group names. If both switches are disabled, there is no valid search scope.

```java
OpenIMClient.getInstance().groupManager.searchGroups(new OnBase<List<GroupInfo>>() {
    @Override public void onSuccess(List<GroupInfo> groups) { showSearchResults(groups); }
    @Override public void onError(int code, String error) { showSearchError(error); }
}, Collections.singletonList(keyword), true, true);
```

A search result does not prove membership. Before opening the group, call [Check group membership](/sdk/android/group/retrieving-groups/is-join-group) when the current state matters.
