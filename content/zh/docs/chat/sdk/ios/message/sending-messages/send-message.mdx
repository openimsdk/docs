---
title: '发送消息'
description: '使用 iOS SDK 发送待发送消息对象。'
sourcePath: '/sdk/ios/message/sending-messages/send-message'
---

```objc
[[OIMManager manager] sendMessage:message
                           recvID:receiverUserID
                          groupID:nil
                     isOnlineOnly:NO
                  offlinePushInfo:nil
                        onSuccess:^(OIMMessageInfo *sent) {
    if (sent != nil) [messageStore upsert:sent];
}
                       onProgress:^(NSInteger progress) {
    [messageStore updateProgress:progress clientMsgID:message.clientMsgID];
}
                        onFailure:nil];
```

单聊只传 `recvID`，群聊只传 `groupID`。`isOnlineOnly` 消息不进入普通离线历史；`offlinePushInfo` 是可选推送配置。成功 callback 的消息可能为空，非空时按 `clientMsgID` 合并。进度、成功、接收 delegate 和历史查询是不同阶段。
