---
title: '查询群组通话房间'
description: '使用 Flutter SDK 按群组 ID 查询进行中的通话房间。'
sourcePath: '/sdk/flutter/calling/retrieving-call-information/get-room-by-group-id'
---

参数是群组 ID，不是自定义 `roomID`：

```dart
final room = await OpenIM.iMManager.signalingManager
    .signalingGetRoomByGroupID(groupID: groupID);

if (room.roomID?.isNotEmpty == true) {
  renderParticipants(room.participant ?? const []);
}
```

Future 成功返回当前的 `RoomCallingInfo`，包含房间、完整邀请和参与者信息。没有进行中的群通话时，应把错误或空房间视为“当前无可恢复通话”，不要继续使用旧凭据。
