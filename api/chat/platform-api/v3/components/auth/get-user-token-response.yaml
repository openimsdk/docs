additionalProperties: false
description: Issued user token and its configured lifetime.
properties:
  expireTimeSeconds:
    format: int64
    type: integer
    description: User token lifetime in seconds.
  token:
    type: string
    description: Issued user token.
type: object
