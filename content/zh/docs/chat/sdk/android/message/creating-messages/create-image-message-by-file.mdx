---
title: '使用本地路径创建图片消息'
description: '使用绝对路径创建图片消息。'
sourcePath: '/sdk/android/message/creating-messages/create-image-message-by-file'
---

```java
Message message = OpenIMClient.getInstance().messageManager
    .createImageMessageFromFullPath(absolutePath);
```

绝对路径必须可读。本方法只创建待发送对象，普通发送流程负责上传图片。
