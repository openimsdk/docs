---
title: '按 ID 批量获取会话'
description: '使用 getMultipleConversation() 按 conversationID 批量获取会话。'
sourcePath: '/sdk/android/conversation/retrieving-conversations/get-conversations-by-id'
---

使用 `getMultipleConversation()` 根据 `conversationIDs` 查询多个会话。`conversationIDs` 是待查询的会话 ID 列表。

```java
OpenIMClient.getInstance().conversationManager.getMultipleConversation(
    new OnBase<List<ConversationInfo>>() {
        @Override
        public void onSuccess(List<ConversationInfo> conversations) {
            for (ConversationInfo item : conversations) {
                conversationStore.put(item.getConversationID(), item);
            }
        }

        @Override
        public void onError(int code, String error) {
            recordConversationQueryFailure(code, error);
        }
    },
    conversationIDs
);
```

成功回调返回 `List<ConversationInfo>`。
