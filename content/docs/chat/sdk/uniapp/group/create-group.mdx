---
title: 'Create a group'
description: 'OpenIM uni-app / uni-app x SDK guide for Create a group.'
product: 'sdk'
context: 'chat/sdk/uniapp'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-20'
version: 'v4'
platform: 'uniapp'
sourcePath: '/sdk/uniapp/group/create-group'
---

```uts
import { createGroup } from '@/uni_modules/unix-openim-sdk'

const result = await createGroup({ groupInfo: { groupName: 'Project' }, memberUserIDs: ['user_b'] })
```

Use valid group type and member IDs, then merge the returned group/event by `groupID`. Creation is not proof that every optional invite succeeded; reload membership when needed.
