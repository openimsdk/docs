---
title: 'Set member friend request permission'
description: 'Use GroupInfo.applyMemberFriend to control whether users can send friend requests from the group member list.'
product: 'sdk'
context: 'chat/sdk/android'
template: 'guide'
status: 'published'
lastUpdated: '2026-08-10'
version: 'v4'
platform: 'android'
sourcePath: '/sdk/android/group/set-group-member-friend-permission'
---

`applyMemberFriend` is an integer switch: `0` disables friend requests and `1` enables them. This rule only affects requests initiated through the member relationship in this group.

```java
GroupInfo update = new GroupInfo();
update.setGroupID(groupID);
update.setApplyMemberFriend(0);

OpenIMClient.getInstance().groupManager.setGroupInfo(update, new OnBase<String>() {
    @Override public void onSuccess(String data) { markFriendPermissionSubmitted(groupID); }
    @Override public void onError(int code, String error) { showPermissionError(error); }
});
```

After the update, use `onGroupInfoChanged` or a newly retrieved `GroupInfo` as the source of truth.
