---
title: 'Add a user to the blacklist'
description: 'Block a user with the OpenIM React Native SDK.'
product: 'sdk'
context: 'chat/sdk/react-native'
template: 'guide'
status: 'published'
version: 'v4'
platform: 'react-native'
sourcePath: '/sdk/react-native/user/blacklist/add-black'
---

Call `addBlack()` to add a specified user to the current account's blocklist.

```ts
await OpenIMSDK.addBlack({
  toUserID,
  ex,
});
```

## Parameter description

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| `toUserID` | `string` | Yes | User ID to block. |
| `ex` | `string` | No | Blocklist relationship extension information. |

After the user is blocked, `OnBlackAdded` is emitted. See [Get the blocklist](/sdk/react-native/user/blacklist/get-black-list) for the blocklist and listener example.
