---
title: 'Create a text message'
description: 'Use createTextMessage to create a text Message.'
product: 'sdk'
context: 'chat/sdk/android'
template: 'guide'
status: 'published'
lastUpdated: '2026-08-10'
version: 'v4'
platform: 'android'
sourcePath: '/sdk/android/message/creating-messages/create-text-message'
---

`createTextMessage()` accepts non-empty text and creates a pending `Message`; it does not send it. Apply product length limits and content checks before the call.

```java
Message message = OpenIMClient.getInstance().messageManager.createTextMessage(text);
```

Add the returned `Message` to a local send queue if needed, then deliver it with [Send a message](/sdk/android/message/sending-messages/send-message). The send callback determines whether delivery from this client succeeded.
