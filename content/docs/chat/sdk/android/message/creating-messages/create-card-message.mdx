---
title: 'Create a contact card message'
description: 'Use createCardMessage to create a contact card message.'
product: 'sdk'
context: 'chat/sdk/android'
template: 'guide'
status: 'published'
lastUpdated: '2026-08-10'
version: 'v4'
platform: 'android'
sourcePath: '/sdk/android/message/creating-messages/create-card-message'
---

`CardElem` stores the user ID, nickname, avatar, and other display data for the contact card. Include only fields that recipients may view; do not substitute a private friend remark or in-group nickname for the public profile.

```java
Message message = OpenIMClient.getInstance().messageManager.createCardMessage(cardElem);
```

The returned `Message` is only a local pending object. This method does not send the card or refresh its user profile. Continue with [Send a message](/sdk/android/message/sending-messages/send-message).
