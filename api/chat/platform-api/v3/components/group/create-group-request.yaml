additionalProperties: false
properties:
  adminUserIDs:
    items:
      type: string
    type: array
  groupInfo:
    allOf:
      - $ref: ../sdkws/group-info.yaml
      - type: object
        required:
          - groupType
        properties:
          groupType:
            enum:
              - 2
  memberUserIDs:
    items:
      type: string
    type: array
  ownerUserID:
    type: string
    minLength: 1
  sendMessage:
    type: boolean
type: object
required:
  - groupInfo
  - ownerUserID
