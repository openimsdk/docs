---
title: 'Delete a friend'
description: 'Use deleteFriend() to remove one friendship from the current user.'
product: 'sdk'
context: 'chat/sdk/android'
template: 'guide'
status: 'published'
lastUpdated: '2026-08-06'
version: 'v4'
platform: 'android'
sourcePath: '/sdk/android/user/friends/delete-friend'
---

```java
OpenIMClient.getInstance().friendshipManager.deleteFriend(
    new OnBase<String>() {
        @Override
        public void onSuccess(String data) {
            // Friend deletion succeeded.
        }

        @Override
        public void onError(int code, String error) {
            // Handle the error.
        }
    },
    friendUserID
);
```

A successful callback means the delete-friend request completed. Receive relationship changes through `onFriendDeleted`; see [Get the friend list by page](/sdk/android/user/friends/get-friend-list-page).
