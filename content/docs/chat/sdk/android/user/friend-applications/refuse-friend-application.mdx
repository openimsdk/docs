---
title: 'Reject a friend request'
description: 'Use refuseFriendApplication() to reject a friend request.'
product: 'sdk'
context: 'chat/sdk/android'
template: 'guide'
status: 'published'
lastUpdated: '2026-08-06'
version: 'v4'
platform: 'android'
sourcePath: '/sdk/android/user/friend-applications/refuse-friend-application'
---

```java
OpenIMClient.getInstance().friendshipManager.refuseFriendApplication(
    new OnBase<String>() {
        @Override
        public void onSuccess(String data) {
            // Friend request rejected successfully.
        }

        @Override
        public void onError(int code, String error) {
            // Handle the error.
        }
    },
    applicantUserID,
    "Not now"
);
```

`applicantUserID` identifies the applicant, and the final argument is the handling message. A successful callback means the reject-friend-request call completed. `OnFriendshipListener` reports the application status; `handleResult == 2` means rejected.
