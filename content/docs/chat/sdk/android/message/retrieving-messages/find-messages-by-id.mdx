---
title: 'Find messages by ID'
description: 'Use findMessageList to retrieve message records with SearchParams.'
product: 'sdk'
context: 'chat/sdk/android'
template: 'guide'
status: 'published'
lastUpdated: '2026-08-10'
version: 'v4'
platform: 'android'
sourcePath: '/sdk/android/message/retrieving-messages/find-messages-by-id'
---

Create a list of `SearchParams` for the messages you need. Merge returned records by message ID rather than treating their list positions as stable identifiers.

```java
messageManager.findMessageList(new OnBase<SearchResult>() {
    @Override public void onSuccess(SearchResult result) { showFoundMessages(result); }
    @Override public void onError(int code, String error) { showQueryError(error); }
}, searchParams);
```
