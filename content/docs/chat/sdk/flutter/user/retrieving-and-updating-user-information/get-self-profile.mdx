---
title: 'Retrieve the current user’s profile'
description: 'Retrieve the current logged-in user’s profile with the OpenIM Flutter SDK.'
product: 'sdk'
context: 'chat/sdk/flutter'
template: 'guide'
status: 'published'
lastUpdated: '2026-08-04'
version: 'v4'
platform: 'flutter'
sourcePath: '/sdk/flutter/user/retrieving-and-updating-user-information/get-self-profile'
---

```dart
final user = await OpenIM.iMManager.userManager.getSelfUserInfo();
setCurrentUser(user);
```

The `Future` returns the current account's `UserInfo`. Use it to initialize the avatar, nickname, and settings UI. This query does not trigger a profile callback. Clear the previous snapshot when the user logs out or switches accounts.
