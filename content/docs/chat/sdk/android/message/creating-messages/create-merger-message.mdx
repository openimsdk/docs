---
title: 'Create a merged message'
description: 'Use createMergerMessage to combine multiple messages into one summary message.'
product: 'sdk'
context: 'chat/sdk/android'
template: 'guide'
status: 'published'
lastUpdated: '2026-08-10'
version: 'v4'
platform: 'android'
sourcePath: '/sdk/android/message/creating-messages/create-merger-message'
---

Pass the message list, title, and summary list.

## Parameters

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| `messages` | `List<Message>` | Yes | Messages to display in the merged item, in the intended order. |
| `title` | `String` | Yes | Title of the merged-message card. |
| `summaries` | `List<String>` | Yes | Summaries used in the card or conversation list, ordered consistently with the content. |

```java
Message message = OpenIMClient.getInstance().messageManager.createMergerMessage(
    messages, title, summaries);
```

The returned `Message` is only a local pending object. Filter messages that cannot be forwarded or contain sensitive content, and limit the item count and summary length before continuing with [Send a message](/sdk/android/message/sending-messages/send-message).
