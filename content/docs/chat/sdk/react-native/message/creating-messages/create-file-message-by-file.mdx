---
title: 'Create a file message from a file'
description: 'OpenIM React Native SDK guide for create a file message from a file.'
product: 'sdk'
context: 'chat/sdk/react-native'
template: 'guide'
status: 'draft'
version: 'v4'
platform: 'react-native'
sourcePath: '/sdk/react-native/message/creating-messages/create-file-message-by-file'
---

Use `createFileMessageFromFullPath()` to create a message from a file on the device.

```ts
const message = await OpenIMSDK.createFileMessageFromFullPath({
  filePath,
  fileName: 'product-guide.pdf',
});
```

## Parameters

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| `filePath` | `string` | Yes | Full path to the file. |
| `fileName` | `string` | Yes | File name shown to recipients. |

The call returns a `MessageItem` ready to send. When you use [Send a message](/sdk/react-native/message/sending-messages/send-message), the SDK uploads the local file.
