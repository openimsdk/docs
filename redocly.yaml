extends:
  - recommended-strict

rules:
  # OpenIM reports validation and business failures in the HTTP 200 API envelope.
  operation-4xx-response: off
  # The Prometheus and object access routes are redirect-only GET operations.
  operation-2xx-response: off
