additionalProperties: false
properties:
  addSource:
    description: 'Method used to add the user to the blacklist.'
    format: int32
    type: integer
  blackUserInfo:
    $ref: public-user-info.yaml
  createTime:
    description: 'Time when the record was created.'
    format: int64
    type: integer
  ex:
    description: 'Application-defined extension data.'
    type: string
  operatorUserID:
    description: 'Identifier of the user who performed the blacklist action.'
    type: string
  ownerUserID:
    description: 'Identifier of the user who owns the record.'
    type: string
type: object
