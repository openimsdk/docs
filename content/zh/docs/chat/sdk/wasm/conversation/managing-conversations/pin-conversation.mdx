---
title: '置顶或取消置顶会话'
description: '设置指定会话在当前用户会话列表中的置顶状态。'
sourcePath: '/sdk/wasm/conversation/managing-conversations/pin-conversation'
---

`isPinned` 只改变当前用户会话列表中的排序状态，不会影响其他群成员或单聊对方。

```ts
await openimsdk.setConversation({
  conversationID,
  isPinned: true,
});
```

传入 `true` 置顶，传入 `false` 取消置顶。Promise 成功表示设置请求完成；收到 `OnConversationChanged` 后，使用 `conversationID` 更新对应会话，完整监听示例见[获取会话列表](/sdk/wasm/conversation/retrieving-conversations/retrieve-conversation-list)。
